name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version-file: .go-version
      - run: go mod download
      - run: go build -v ./...

  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version-file: .go-version
      - uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  generate:
    name: Generate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version-file: .go-version
      - run: go generate ./...
      - run: cd tools && go generate ./...
      - name: Verify no generated changes
        run: |
          git diff --compact-summary --exit-code || \
            (echo "::error::Uncommitted changes after running go generate. Run 'make generate' and commit the results." && exit 1)

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with:
          go-version-file: .go-version
      - run: go test -v -cover -timeout=120s -parallel=10 ./...

  # Acceptance tests require JumpCloud API credentials and are run manually
  # or on a schedule. They are not run on every PR.
  # To run locally: JUMPCLOUD_API_KEY=xxx make testacc
